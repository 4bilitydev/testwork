<%= form_with(model: schoolconnect, local: true) do |form| %>
  <% if schoolconnect.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(schoolconnect.errors.count, "error") %> prohibited this schoolconnect from being saved:</h2>

      <ul>
      <% schoolconnect.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :schoolconnect_title %>
  </div>

  <div class="field">
    <%= form.label :content %>
    <%= form.text_area :content, id: :schoolconnect_content %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
